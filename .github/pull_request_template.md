## Summary

- Scope:
- Why:

## Lane

- [ ] Sentinel desktop (`apps/sentinel-desktop`)
- [ ] Mission control (`apps/mission-control`)
- [ ] Bridge contract (`services/bridge-api` / `shared/schemas`)

## Validation

- [ ] Local run flow works (`run-bridge`, `run-sentinel`, `run-mission-control`)
- [ ] `python scripts/smoke_check.py` passes
- [ ] If contract changed, both sentinel and mission-control were verified

## Screens / Evidence

- Capture or logs:
